[gcs]
type = google cloud storage
service_account_file = /etc/juicefs/sa-private-key.json
# Bucket has uniform bucket-level access; without this, rclone tries to set
# per-object ACLs and gets HTTP 400.
bucket_policy_only = true
