[Unit]
After=redis-server.service
# Wants= (not Requires=) so a redis-server restart does not cascade into
# a /storage unmount attempt. After= still gives correct boot ordering.
Wants=redis-server.service
